import { IStyles } from '@/types/global';

export const styles = {
  headerContainer: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    px: 10,
    py: 4,
  },
  heading: { fontSize: '35px' },
} satisfies IStyles;
